server:
  port: 8761

spring:
  application:
    name: eureka-discovery-service

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
